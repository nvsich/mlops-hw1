# MLOps Homework 1

Система для обучения и управления ML-моделями с REST и gRPC API, интеграцией с ClearML и DVC, и интерактивным дашбордом на Streamlit.

### Модели

Система поддерживает 2 класса моделей:
- **LinearRegression** - Линейная регрессия
- **RandomForest** - Случайный лес

Каждая модель поддерживает настройку гиперпараметров через JSON.

### Интеграции

- **ClearML** - Для экспериментов и хранения моделей
- **DVC** - Для версионирования датасетов
- **MinIO (S3)** - Для хранения весов и кэша

## Установка и запуск

#### Для Linux/Mac:

#### Для Windows:

```bash
chmod +x setup.sh
./setup.sh
```

(на unix системах не было возможности проверить)
Запуск всего стека одной командой:

```bash
make all
```

Или используйте скрипт:

```bash
chmod +x setup.sh
./setup.sh
```

### Доступ к сервисам

После развертывания:

- **Dashboard**: URL будет выведен после `make deploy` или:
  ```bash
  minikube service mlops-dashboard -n mlops
  ```

## Использование

### Через Dashboard

1. Откройте дашборд в браузере
2. Вкладка **Datasets**: загрузите CSV или JSON датасет
3. Вкладка **Training**: выберите модель, датасет, укажите гиперпараметры в JSON и обучите
4. Вкладка **Inference**: выберите модель и получите предсказания


## Формат датасетов

Датасеты должны быть в формате CSV или JSON. CSV должен содержать заголовки, JSON должен быть массивом объектов. Обязательно наличие колонки с целевой переменной (по умолчанию "target").

## Логгирование

Все важные действия логируются через стандартный Python logging. Логи доступны через:

```bash
kubectl logs -n mlops deployment/mlops-service
```